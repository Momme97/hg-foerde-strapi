'use strict';

/**
 *  news-auswahlkader controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::news-auswahlkader.news-auswahlkader');
