'use strict';

/**
 * news-auswahlkader service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::news-auswahlkader.news-auswahlkader');
