'use strict';

/**
 *  schiedsrichter-fortbildung controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::schiedsrichter-fortbildung.schiedsrichter-fortbildung');
