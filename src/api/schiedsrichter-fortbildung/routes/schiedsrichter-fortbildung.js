'use strict';

/**
 * schiedsrichter-fortbildung router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::schiedsrichter-fortbildung.schiedsrichter-fortbildung');
