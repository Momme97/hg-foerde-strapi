'use strict';

/**
 * schiedsrichter-fortbildung service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::schiedsrichter-fortbildung.schiedsrichter-fortbildung');
