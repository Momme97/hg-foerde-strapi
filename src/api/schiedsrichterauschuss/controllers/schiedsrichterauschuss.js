'use strict';

/**
 *  schiedsrichterauschuss controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::schiedsrichterauschuss.schiedsrichterauschuss');
