'use strict';

/**
 * schiedsrichterauschuss router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::schiedsrichterauschuss.schiedsrichterauschuss');
